class Product {
  constructor(id, image, title, price, details, categoryId) {
    this.id = id;
    this.image = image;
    this.title = title;
    this.price = price;
    this.details = details;
    this.categoryId = categoryId;
  }
};

export default Product;
